# Assumptions

- User wants all actionable code changes from `NIRI_PYPC_PYDANTIC_V2_CODE_REVIEW.md` implemented.
- Exception: do not add GitHub Actions workflow files; provide CI-equivalent script command(s) in `devenv.nix` instead.
- Existing dirty worktree entries in `.scratch/projects/09-pydantic-refactor-code-review/` are user-owned and should not be reverted.
- "Commit and push after each step" means after each implementation phase in this plan.
