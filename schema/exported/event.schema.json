{
  "$defs": {
    "KeyboardLayouts": {
      "description": "Configured keyboard layouts.",
      "properties": {
        "current_idx": {
          "description": "Index of the currently active layout in `names`.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "names": {
          "description": "XKB names of the configured layouts.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "names",
        "current_idx"
      ],
      "type": "object"
    },
    "Timestamp": {
      "description": "A moment in time.",
      "properties": {
        "nanos": {
          "description": "Fractional part of the timestamp in nanoseconds (10<sup>-9</sup> seconds).",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "secs": {
          "description": "Number of whole seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "secs",
        "nanos"
      ],
      "type": "object"
    },
    "Window": {
      "description": "Toplevel window.",
      "properties": {
        "app_id": {
          "description": "Application ID, if set.",
          "type": [
            "string",
            "null"
          ]
        },
        "focus_timestamp": {
          "anyOf": [
            {
              "$ref": "#/$defs/Timestamp"
            },
            {
              "type": "null"
            }
          ],
          "description": "Timestamp when the window was most recently focused.\n\nThis timestamp is intended for most-recently-used window switchers, i.e. Alt-Tab. It only\nupdates after some debounce time so that quick window switching doesn't mark intermediate\nwindows as recently focused.\n\nThe timestamp comes from the monotonic clock."
        },
        "id": {
          "description": "Unique id of this window.\n\nThis id remains constant while this window is open.\n\nDo not assume that window ids will always increase without wrapping, or start at 1. That is\nan implementation detail subject to change. For example, ids may change to be randomly\ngenerated for each new window.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "is_floating": {
          "description": "Whether this window is currently floating.\n\nIf the window isn't floating then it is in the tiling layout.",
          "type": "boolean"
        },
        "is_focused": {
          "description": "Whether this window is currently focused.\n\nThere can be either one focused window or zero (e.g. when a layer-shell surface has focus).",
          "type": "boolean"
        },
        "is_urgent": {
          "description": "Whether this window requests your attention.",
          "type": "boolean"
        },
        "layout": {
          "$ref": "#/$defs/WindowLayout",
          "description": "Position- and size-related properties of the window."
        },
        "pid": {
          "description": "Process ID that created the Wayland connection for this window, if known.\n\nCurrently, windows created by xdg-desktop-portal-gnome will have a `None` PID, but this may\nchange in the future.",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "title": {
          "description": "Title, if set.",
          "type": [
            "string",
            "null"
          ]
        },
        "workspace_id": {
          "description": "Id of the workspace this window is on, if any.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "is_focused",
        "is_floating",
        "is_urgent",
        "layout"
      ],
      "type": "object"
    },
    "WindowLayout": {
      "description": "Position- and size-related properties of a [`Window`].\n\nOptional properties will be unset for some windows, do not rely on them being present. Whether\nsome optional properties are present or absent for certain window types may change across niri\nreleases.\n\nAll sizes and positions are in *logical pixels* unless stated otherwise. Logical sizes may be\nfractional. For example, at 1.25 monitor scale, a 2-physical-pixel-wide window border is 1.6\nlogical pixels wide.\n\nThis struct contains positions and sizes both for full tiles ([`Self::tile_size`],\n[`Self::tile_pos_in_workspace_view`]) and the window geometry ([`Self::window_size`],\n[`Self::window_offset_in_tile`]). For visual displays, use the tile properties, as they\ncorrespond to what the user visually considers \"window\". The window properties on the other\nhand are mainly useful when you need to know the underlying Wayland window sizes, e.g. for\napplication debugging.",
      "properties": {
        "pos_in_scrolling_layout": {
          "description": "Location of a tiled window within a workspace: (column index, tile index in column).\n\nThe indices are 1-based, i.e. the leftmost column is at index 1 and the topmost tile in a\ncolumn is at index 1. This is consistent with [`Action::FocusColumn`] and\n[`Action::FocusWindowInColumn`].",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            },
            {
              "format": "uint",
              "minimum": 0,
              "type": "integer"
            }
          ],
          "type": [
            "array",
            "null"
          ]
        },
        "tile_pos_in_workspace_view": {
          "description": "Tile position within the current view of the workspace.\n\nThis is the same \"workspace view\" as in gradients' `relative-to` in the niri config.",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "format": "double",
              "type": "number"
            },
            {
              "format": "double",
              "type": "number"
            }
          ],
          "type": [
            "array",
            "null"
          ]
        },
        "tile_size": {
          "description": "Size of the tile this window is in, including decorations like borders.",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "format": "double",
              "type": "number"
            },
            {
              "format": "double",
              "type": "number"
            }
          ],
          "type": "array"
        },
        "window_offset_in_tile": {
          "description": "Location of the window's visual geometry within its tile.\n\nThis includes things like border sizes. For fullscreened fixed-size windows this includes\nthe distance from the corner of the black backdrop to the corner of the (centered) window\ncontents.",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "format": "double",
              "type": "number"
            },
            {
              "format": "double",
              "type": "number"
            }
          ],
          "type": "array"
        },
        "window_size": {
          "description": "Size of the window's visual geometry itself.\n\nDoes not include niri decorations like borders.\n\nCurrently, Wayland toplevel windows can only be integer-sized in logical pixels, even\nthough it doesn't necessarily align to physical pixels.",
          "maxItems": 2,
          "minItems": 2,
          "prefixItems": [
            {
              "format": "int32",
              "type": "integer"
            },
            {
              "format": "int32",
              "type": "integer"
            }
          ],
          "type": "array"
        }
      },
      "required": [
        "tile_size",
        "window_size",
        "window_offset_in_tile"
      ],
      "type": "object"
    },
    "Workspace": {
      "description": "A workspace.",
      "properties": {
        "active_window_id": {
          "description": "Id of the active window on this workspace, if any.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "id": {
          "description": "Unique id of this workspace.\n\nThis id remains constant regardless of the workspace moving around and across monitors.\n\nDo not assume that workspace ids will always increase without wrapping, or start at 1. That\nis an implementation detail subject to change. For example, ids may change to be randomly\ngenerated for each new workspace.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "idx": {
          "description": "Index of the workspace on its monitor.\n\nThis is the same index you can use for requests like `niri msg action focus-workspace`.\n\nThis index *will change* as you move and re-order workspace. It is merely the workspace's\ncurrent position on its monitor. Workspaces on different monitors can have the same index.\n\nIf you need a unique workspace id that doesn't change, see [`Self::id`].",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "is_active": {
          "description": "Whether the workspace is currently active on its output.\n\nEvery output has one active workspace, the one that is currently visible on that output.",
          "type": "boolean"
        },
        "is_focused": {
          "description": "Whether the workspace is currently focused.\n\nThere's only one focused workspace across all outputs.",
          "type": "boolean"
        },
        "is_urgent": {
          "description": "Whether the workspace currently has an urgent window in its output.",
          "type": "boolean"
        },
        "name": {
          "description": "Optional name of the workspace.",
          "type": [
            "string",
            "null"
          ]
        },
        "output": {
          "description": "Name of the output that the workspace is on.\n\nCan be `None` if no outputs are currently connected.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id",
        "idx",
        "is_urgent",
        "is_active",
        "is_focused"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A compositor event.",
  "oneOf": [
    {
      "additionalProperties": false,
      "description": "The workspace configuration has changed.",
      "properties": {
        "WorkspacesChanged": {
          "properties": {
            "workspaces": {
              "description": "The new workspace configuration.\n\nThis configuration completely replaces the previous configuration. I.e. if any\nworkspaces are missing from here, then they were deleted.",
              "items": {
                "$ref": "#/$defs/Workspace"
              },
              "type": "array"
            }
          },
          "required": [
            "workspaces"
          ],
          "type": "object"
        }
      },
      "required": [
        "WorkspacesChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The workspace urgency changed.",
      "properties": {
        "WorkspaceUrgencyChanged": {
          "properties": {
            "id": {
              "description": "Id of the workspace.",
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "urgent": {
              "description": "Whether this workspace has an urgent window.",
              "type": "boolean"
            }
          },
          "required": [
            "id",
            "urgent"
          ],
          "type": "object"
        }
      },
      "required": [
        "WorkspaceUrgencyChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "A workspace was activated on an output.\n\nThis doesn't always mean the workspace became focused, just that it's now the active\nworkspace on its output. All other workspaces on the same output become inactive.",
      "properties": {
        "WorkspaceActivated": {
          "properties": {
            "focused": {
              "description": "Whether this workspace also became focused.\n\nIf `true`, this is now the single focused workspace. All other workspaces are no longer\nfocused, but they may remain active on their respective outputs.",
              "type": "boolean"
            },
            "id": {
              "description": "Id of the newly active workspace.",
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "id",
            "focused"
          ],
          "type": "object"
        }
      },
      "required": [
        "WorkspaceActivated"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "An active window changed on a workspace.",
      "properties": {
        "WorkspaceActiveWindowChanged": {
          "properties": {
            "active_window_id": {
              "description": "Id of the new active window, if any.",
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "workspace_id": {
              "description": "Id of the workspace on which the active window changed.",
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "workspace_id"
          ],
          "type": "object"
        }
      },
      "required": [
        "WorkspaceActiveWindowChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The window configuration has changed.",
      "properties": {
        "WindowsChanged": {
          "properties": {
            "windows": {
              "description": "The new window configuration.\n\nThis configuration completely replaces the previous configuration. I.e. if any windows\nare missing from here, then they were closed.",
              "items": {
                "$ref": "#/$defs/Window"
              },
              "type": "array"
            }
          },
          "required": [
            "windows"
          ],
          "type": "object"
        }
      },
      "required": [
        "WindowsChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "A new toplevel window was opened, or an existing toplevel window changed.",
      "properties": {
        "WindowOpenedOrChanged": {
          "properties": {
            "window": {
              "$ref": "#/$defs/Window",
              "description": "The new or updated window.\n\nIf the window is focused, all other windows are no longer focused."
            }
          },
          "required": [
            "window"
          ],
          "type": "object"
        }
      },
      "required": [
        "WindowOpenedOrChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "A toplevel window was closed.",
      "properties": {
        "WindowClosed": {
          "properties": {
            "id": {
              "description": "Id of the removed window.",
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      },
      "required": [
        "WindowClosed"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Window focus changed.\n\nAll other windows are no longer focused.",
      "properties": {
        "WindowFocusChanged": {
          "properties": {
            "id": {
              "description": "Id of the newly focused window, or `None` if no window is now focused.",
              "format": "uint64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "required": [
        "WindowFocusChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Window focus timestamp changed.\n\nThis event is separate from [`Event::WindowFocusChanged`] because the focus timestamp only\nupdates after some debounce time so that quick window switching doesn't mark intermediate\nwindows as recently focused.",
      "properties": {
        "WindowFocusTimestampChanged": {
          "properties": {
            "focus_timestamp": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Timestamp"
                },
                {
                  "type": "null"
                }
              ],
              "description": "The new focus timestamp."
            },
            "id": {
              "description": "Id of the window.",
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      },
      "required": [
        "WindowFocusTimestampChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "Window urgency changed.",
      "properties": {
        "WindowUrgencyChanged": {
          "properties": {
            "id": {
              "description": "Id of the window.",
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "urgent": {
              "description": "The new urgency state of the window.",
              "type": "boolean"
            }
          },
          "required": [
            "id",
            "urgent"
          ],
          "type": "object"
        }
      },
      "required": [
        "WindowUrgencyChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The layout of one or more windows has changed.",
      "properties": {
        "WindowLayoutsChanged": {
          "properties": {
            "changes": {
              "description": "Pairs consisting of a window id and new layout information for the window.",
              "items": {
                "maxItems": 2,
                "minItems": 2,
                "prefixItems": [
                  {
                    "format": "uint64",
                    "minimum": 0,
                    "type": "integer"
                  },
                  {
                    "$ref": "#/$defs/WindowLayout"
                  }
                ],
                "type": "array"
              },
              "type": "array"
            }
          },
          "required": [
            "changes"
          ],
          "type": "object"
        }
      },
      "required": [
        "WindowLayoutsChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The configured keyboard layouts have changed.",
      "properties": {
        "KeyboardLayoutsChanged": {
          "properties": {
            "keyboard_layouts": {
              "$ref": "#/$defs/KeyboardLayouts",
              "description": "The new keyboard layout configuration."
            }
          },
          "required": [
            "keyboard_layouts"
          ],
          "type": "object"
        }
      },
      "required": [
        "KeyboardLayoutsChanged"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The keyboard layout switched.",
      "properties": {
        "KeyboardLayoutSwitched": {
          "properties": {
            "idx": {
              "description": "Index of the newly active layout.",
              "format": "uint8",
              "maximum": 255,
              "minimum": 0,
              "type": "integer"
            }
          },
          "required": [
            "idx"
          ],
          "type": "object"
        }
      },
      "required": [
        "KeyboardLayoutSwitched"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The overview was opened or closed.",
      "properties": {
        "OverviewOpenedOrClosed": {
          "properties": {
            "is_open": {
              "description": "The new state of the overview.",
              "type": "boolean"
            }
          },
          "required": [
            "is_open"
          ],
          "type": "object"
        }
      },
      "required": [
        "OverviewOpenedOrClosed"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "The configuration was reloaded.\n\nYou will always receive this event when connecting to the event stream, indicating the last\nconfig load attempt.",
      "properties": {
        "ConfigLoaded": {
          "properties": {
            "failed": {
              "description": "Whether the loading failed.\n\nFor example, the config file couldn't be parsed.",
              "type": "boolean"
            }
          },
          "required": [
            "failed"
          ],
          "type": "object"
        }
      },
      "required": [
        "ConfigLoaded"
      ],
      "type": "object"
    },
    {
      "additionalProperties": false,
      "description": "A screenshot was captured.",
      "properties": {
        "ScreenshotCaptured": {
          "properties": {
            "path": {
              "description": "The file path where the screenshot was saved, if it was written to disk.\n\nIf `None`, the screenshot was either only copied to the clipboard, or the path couldn't\nbe converted to a `String` (e.g. contained invalid UTF-8 bytes).",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "required": [
        "ScreenshotCaptured"
      ],
      "type": "object"
    }
  ],
  "title": "Event"
}